import { EnTranslation } from "~/locale/common/translations/en";
import type { i18nCatalog } from "~/locale/common/types";

export const catalog = {
	en: EnTranslation,
} satisfies i18nCatalog;
